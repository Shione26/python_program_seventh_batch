# Prog08. count() return how many time the function parameter appear in the string. Create a program that do the same functionality without using count() function.

# ask the user for input
text = input("Enter text: ")
# ask the user what character to count
character = input("Enter character to count: ")
# initialize a variable to store the count
count = 0
# check each character in text if it matches the character to count
# if yes, increment variable
# print output